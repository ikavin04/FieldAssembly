"""Voice token endpoint — provides temporary AssemblyAI session tokens."""

from flask import Blueprint

voice_bp = Blueprint("voice", __name__)

# TODO: GET /api/voice-token — generate temporary AssemblyAI token
